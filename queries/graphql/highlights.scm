;; extends

(object_type_extension (name) @toplevel-declaration)

(object_type_definition (name) @toplevel-declaration)

(input_object_type_definition (name) @toplevel-declaration)

(enum_type_definition (name) @toplevel-declaration)

(input_value_definition (name) @parameter-decl)

(field_definition (name) @parameter-decl)
